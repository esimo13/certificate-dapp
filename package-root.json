{
  "name": "certificate-verification-dapp",
  "version": "1.0.0",
  "description": "Decentralized certificate verification system using blockchain",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspaces",
    "start:frontend": "npm run dev --workspace=frontend",
    "start:backend": "npm run dev --workspace=backend",
    "deploy:contracts": "npm run deploy --workspace=contracts",
    "blockchain": "npm run node --workspace=contracts",
    "setup": "npm install && npm run install --workspaces"
  },
  "keywords": [
    "blockchain",
    "certificate",
    "verification",
    "ethereum",
    "nextjs",
    "dapp"
  ],
  "author": "Certificate DApp Team",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
